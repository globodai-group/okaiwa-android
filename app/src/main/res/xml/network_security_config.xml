<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        Base config — no cleartext traffic anywhere.
        All Okaiwa traffic goes through HTTPS to the production relay.
    -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!--
        Debug override — allow user-installed CAs so developers can
        inspect traffic with Charles/Proxyman during local testing.
    -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
